apiVersion: fungi.rs/v1alpha1
kind: Service

metadata:
  name: webdav

spec:
  run:
    wasmtime:
      url: https://github.com/enbop/webdav-wasi/releases/download/v0.1.1/webdav-wasi.wasm

  entries:
    http:
      port: 8080
      usage: tcp

  mounts:
    - hostPath: ${USER_HOME}
      runtimePath: data

  env:
    WEBDAV_FS_ROOT: data

notes:
  - "Project: https://github.com/enbop/webdav-wasi"
  - "Artifact URL: https://github.com/enbop/webdav-wasi/releases/download/v0.1.1/webdav-wasi.wasm"
  - "Use a WebDAV client against the printed local address; browser GET currently returns a Wasmtime 500 for this experimental component."
